<%
title = 'Pricing';
%>
<%- contentFor('body') %>
<div class="dark:bg-[#0a0a0a] rounded-2xl">
    <div class="max-w-7xl mx-auto py-24 px-4 sm:px-6 lg:px-8">
        <div class="text-center">
            <h2 class="text-3xl font-medium text-white sm:text-4xl">
                Simple, transparent pricing
            </h2>
            <p class="mt-4 text-lg text-gray-400">
                Choose the plan that's right for your needs
            </p>
        </div>

        <div class="mt-12 space-y-4 sm:mt-16 sm:space-y-0 sm:grid sm:grid-cols-2 sm:gap-6 lg:max-w-4xl lg:mx-auto xl:max-w-none xl:mx-0 xl:grid-cols-3">
            <% plans.forEach(plan => { %>
                <div class="relative rounded-2xl <%= plan.code === currentPlanCode ? 'border-2 border-purple-500' : 'border border-gray-200 dark:border-[#27272a]' %> p-8 shadow-sm">
                    <div class="absolute inset-0 bg-gradient-to-r from-[#6B3BFA] to-[#F11E76] rounded-2xl blur-3xl opacity-10"></div>
                    <div class="relative">
                        <h3 class="text-lg font-semibold leading-8 text-gray-900 dark:text-white">
                            <%= plan.name %>
                            <% if (plan.code === currentPlanCode) { %>
                                <span class="ml-2 text-sm text-[#6B3BFA]">(Current Plan)</span>
                            <% } %>
                        </h3>
                        
                        <p class="mt-6 flex items-baseline gap-x-1">
                            <span class="text-4xl font-bold tracking-tight text-gray-900 dark:text-white">$<%= plan.price %></span>
                            <span class="text-sm font-semibold leading-6 text-gray-600 dark:text-gray-400">/<%= plan.interval %></span>
                        </p>
                        <% if (user) { %>
                            <% if (plan.code === currentPlanCode) { %>
                                <div class="mt-8 block w-full bg-[#6B3BFA] text-white rounded-md py-2 text-sm font-semibold text-center">
                                    Current Plan
                                </div>
                            <% } else { %>
                                <a href="/settings/billing"
                                    class="mt-8 block w-full <%= plan.code === 'pro' ? 'bg-neon-blue text-black hover:bg-blue-400' : 'bg-dark-700 text-white hover:bg-dark-600' %> border border-transparent rounded-md py-2 text-sm font-semibold text-center">
                                    <%= plan.code === 'enterprise' ? 'Contact sales' : (plan.price > 0 ? 'Upgrade' : 'Downgrade') %>
                                </a>
                            <% } %>
                        <% } else { %>
                            <a href="/register"
                                class="mt-8 block w-full <%= plan.code === 'pro' ? 'bg-neon-blue text-black hover:bg-blue-400' : 'bg-dark-700 text-white hover:bg-dark-600' %> border border-transparent rounded-md py-2 text-sm font-semibold text-center">
                                <%= plan.code === 'enterprise' ? 'Contact sales' : 'Get started' %>
                            </a>
                        <% } %>

                        <div class="mt-8 pt-8 border-t border-gray-200 dark:border-[#27272a]">
                            <h4 class="text-sm font-semibold text-gray-900 dark:text-white tracking-wide uppercase">What's included</h4>
                            <ul role="list" class="mt-6 space-y-4">
                                <% plan.features.forEach(feature => { %>
                                    <li class="flex space-x-3">
                                        <svg class="flex-shrink-0 h-5 w-5 text-[#6B3BFA]" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                                        </svg>
                                        <span class="text-sm text-gray-600 dark:text-gray-400"><%= feature %></span>
                                    </li>
                                <% }); %>
                            </ul>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>

        <!-- FAQ Section -->
        <div class="mt-24">
            <h2 class="text-3xl font-medium text-white text-center">Frequently asked questions</h2>
            <div class="mt-12 max-w-3xl mx-auto divide-y divide-gray-700">
                <dl class="mt-6 space-y-6 divide-y divide-gray-700">
                    <div class="pt-6">
                        <dt class="text-lg">
                            <span class="font-medium text-white">What's included in the free plan?</span>
                        </dt>
                        <dd class="mt-2 text-base font-mono text-gray-400">
                            The free plan includes one status page with basic components and analytics. Perfect for small projects or testing the service.
                        </dd>
                    </div>

                    <div class="pt-6">
                        <dt class="text-lg">
                            <span class="font-medium text-white">Can I upgrade or downgrade my plan?</span>
                        </dt>
                        <dd class="mt-2 text-base font-mono text-gray-400">
                            Yes, you can change your plan at any time. When upgrading, you'll be charged the difference for the remainder of your billing period.
                        </dd>
                    </div>

                    <div class="pt-6">
                        <dt class="text-lg">
                            <span class="font-medium text-white">What payment methods do you accept?</span>
                        </dt>
                        <dd class="mt-2 text-base font-mono text-gray-400">
                            We accept all major credit cards, PayPal, and bank transfers for enterprise plans.
                        </dd>
                    </div>

                    <div class="pt-6">
                        <dt class="text-lg">
                            <span class="font-medium text-white">Do you offer refunds?</span>
                        </dt>
                        <dd class="mt-2 text-base font-mono text-gray-400">
                            Yes, we offer a 30-day money-back guarantee for all paid plans. If you're not satisfied, contact our support team.
                        </dd>
                    </div>
                </dl>
            </div>
        </div>
    </div>
</div> 